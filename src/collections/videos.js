var Videos = Backbone.Collection.extend({

  model: Video

});

//collection = new Videos(exampleVideoData);

// var videoList = _.map(exampleVideoData, video => new Video(video) );

// var videos = new Videos(videoList);

// new appView({ collection: videos});
//create a for loop that loads each video
//on each video, add a new in front of video

